server:
  port: 8090

spring:
  application:
    name: organization-service
  profiles:
    active: dev
  #config:
    #import=optional:configserver:http://localhost:8071
  cloud:
    config:
      uri: http://localhost:8071